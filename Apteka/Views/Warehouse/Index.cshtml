@model IEnumerable<Apteka.Models.Hurtownia>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
@section PanelName
{
    Spis hurtowni
}
@section Panel
{


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nazwa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NIP)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nazwa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NIP)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_hurtownia }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_hurtownia }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_hurtownia })
        </td>
    </tr>
}

</table>
}
@section PanelFooter
{
    <p>  </p>
}